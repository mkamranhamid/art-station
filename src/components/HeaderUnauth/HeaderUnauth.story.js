import React from 'react';
import { action } from '@storybook/addon-actions';
import { HeaderUnauth } from './HeaderUnauth'

export default {
    title: 'Header Unauth',
};

export const headerUnauth = () => <HeaderUnauth />;
